# Instructions

- When changing the Toby control JSON-RPC protocol, update `docs/toby-control-openapi.yaml` in the same change.
- When changing `toby mcp` tools, arguments, or setup requirements, update the MCP section in `README.md` in the same change.
